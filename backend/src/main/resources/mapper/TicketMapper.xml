<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ticket.backend.mapper.TicketMapper">

    <!--티켓 전체 조회-->
    <select id="findAll" resultType="com.ticket.backend.domain.Tickets">
        SELECT
        ticket_id,
        ticket_name,
        venue,
        sale_start_at,
        sale_end_at,
        valid_start_at,
        valid_end_at,
        total_quantity,
        remaining_quantity,
        purchase_limit,
        price,
        discount_rate
        FROM tickets
    </select>

    <!--티켓 상세 조회-->
    <select id="findById" parameterType="long" resultType="com.ticket.backend.domain.Tickets">
        SELECT
        ticket_id,
        ticket_name,
        venue,
        sale_start_at,
        sale_end_at,
        valid_start_at,
        valid_end_at,
        total_quantity,
        remaining_quantity,
        purchase_limit,
        price,
        discount_rate
        FROM tickets
        WHERE ticket_id = #{ticketId}
    </select>

    <!--조건부 업데이트-->
    <update id="decreaseStock">
        UPDATE tickets
        SET remaining_quantity = remaining_quantity - #{quantity}
        WHERE ticket_id = #{ticketId}
        AND remaining_quantity >= #{quantity}
    </update>
</mapper>